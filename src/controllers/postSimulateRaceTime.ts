import {Request, Response} from "express";

export const postSimulateRaceTime = (req: Request, res: Response) => {
    // TODO implement postSimulateRaceTime
    // TODO that parameter exists and is > 0
    // TODO calculate new position for every runner
    // TODO get all runners and order them by position
};